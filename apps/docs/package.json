{
  "name": "@via-farm-lab/docs",
  "version": "0.0.0",
  "private": true,
  "description": "Documentation site — VitePress. Implemented from PR 40 (simulator spec doc) and grows from there.",
  "type": "module",
  "scripts": {
    "dev": "echo '@via-farm-lab/docs — to be implemented in PR 40'",
    "build": "echo 'pending'",
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo 'pending'"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
