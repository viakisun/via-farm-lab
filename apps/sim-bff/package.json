{
  "name": "@via-farm-lab/sim-bff",
  "version": "0.0.0",
  "private": true,
  "description": "Simulator BFF — Fastify + DuckDB/Postgres + 5 external API surfaces (Console/Backend/Robot/Subscription/Growth). Implemented from PR 17.",
  "type": "module",
  "scripts": {
    "dev": "echo '@via-farm-lab/sim-bff — to be implemented in PR 17 (Fastify scaffold)'",
    "build": "echo 'pending'",
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo 'pending'"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
