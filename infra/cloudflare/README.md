# infra/cloudflare

Cloudflare configuration — R2 buckets (APAC region) for 3D assets (HDRIs, KTX2 textures, glTF), Pages for PR previews, Workers if needed.

**Status**: placeholder. Initialised at **PR 50** (asset CDN). Cloudflare API token required from then on.
