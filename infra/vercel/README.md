# infra/vercel

Vercel deployment configuration — main web app, PR previews, Sydney edge region.

**Status**: placeholder. Initialised at **PR 167** (production deployment). Vercel token + `.au` domain required from then on.
