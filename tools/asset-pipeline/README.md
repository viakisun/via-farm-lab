# tools/asset-pipeline

Asset pipeline scripts — Blender headless export to glTF (Draco-compressed), KTX2 texture conversion (BasisU / ETC1S / UASTC), lightmap baking workflow (Cycles → KTX2).

**Status**: placeholder. Initialised in PR 40 (lightmap pipeline) and grows through Phase 2 asset PRs (PR 65 onwards).

See [PLAN.md](../../PLAN.md) §6 (external asset dependencies).
