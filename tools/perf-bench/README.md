# tools/perf-bench

GPU and rendering benchmarks — quality-tier auto-detection, FPS / GPU time / memory measurements across Apple Silicon variants (M1 / M1 Pro / M1 Max / M2 / M4).

**Status**: placeholder. Initialised in PR 148 (quality-tier auto-select) and used through Phase 8.
